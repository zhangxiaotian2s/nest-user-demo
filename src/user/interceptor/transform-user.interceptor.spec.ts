import { TransformUserInterceptor } from './transform-user.interceptor';

describe('TransformUserInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformUserInterceptor()).toBeDefined();
  });
});
